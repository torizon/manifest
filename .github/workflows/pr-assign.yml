name: Auto Self-Assign PR

on:
  pull_request: # <-- Changed from pull_request_target for testing
    types: [opened, reopened, synchronized] # <-- Added synchronized to easily re-trigger

permissions:
  pull-requests: write

jobs:
  assign-author:
    runs-on: ubuntu-latest
    steps:
      - name: Assign PR Author
        uses: toshimaru/auto-author-assign@v3.1.0

